import URL from "../../config/url.js";

import OrderService from "../../services/borzo/OrderService.js";
import Logger from "../../util/Logger.js";

const AdController = {
  ads: (req, res) => {},
};

export default AdController;
